{
  "name": "from0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r --filter ./packages/** run build",
    "dev": "pnpm -r --filter ./packages/** --parallel run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.15.12",
    "typescript": "^4.8.4"
  }
}
